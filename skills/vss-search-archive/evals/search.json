{
  "skills": [
    "vss-search-archive",
    "vss-deploy-profile"
  ],
  "profile": "search",
  "deploy_mode": "remote-all",
  "resources": {
    "platforms": {
      "RTXPRO6000BW": {
        "gpu_count": 2
      }
    }
  },
  "expects": [
    {
      "query": "Deploy the VSS **search** profile on `{{platform}}` via `/vss-deploy-profile -p search -m remote-all`. On Brev, make `BREV_ENV_ID` from `/etc/environment` and any platform-provided `BREV_LINK_DOMAIN` available to that existing deployment workflow; do not construct a secure-link hostname yourself. After local agent and VST health succeeds, read the fully expanded `VST_EXTERNAL_URL` from the runtime `generated.env` and require a bounded GET through that exact public origin to succeed before downloading or ingesting sample media; reject localhost, internal IP, and non-HTTPS origins, and stop if deployment routing cannot be repaired within the bounded setup deadline. Resolve setup state exactly once with the skill's `RUNTIME_JSON` recipe (`discover_docker` + `discover_docker_host_endpoints` + `RuntimeSnapshot.from_config_file`) and require the distinct index values `mdx-embed-filtered-2025-01-01`, `mdx-behavior-2025-01-01`, and `mdx-raw-2025-01-01`; never use `ELASTIC_SEARCH_INDEX` for behavior or raw queries. Make setup idempotent on a reused host before upload: list VST through the discovered endpoint, agent-delete exact or deduplicated remnants of `warehouse_sample`, `warehouse-ladder`, and legacy `sample-warehouse-ladder`, require successful cleanup, and wait for their absence. Then download and extract the pinned `nvidia/vss-developer/dev-profile-sample-data:3.2.0` bundle and ingest only the bytes from `warehouse_sample.mp4` and `sample-warehouse-ladder.mp4` through the standard VSS agent-backed three-step workflow: `POST /api/v1/videos`, POST the file bytes to its returned VST upload URL, then `POST /api/v1/videos/{sensor}/complete` and validate that separate response. Never invoke the deprecated single-step `PUT /api/v1/videos-for-search/{filename}` route. For the ladder file, set the canonical upload filename to `warehouse-ladder.mp4` and use it consistently in the upload request, VST chunk headers and multipart fields, and `/complete` body, so its registered source name is `warehouse-ladder`. Check readiness with bounded requests: if the first probe is ready, continue immediately; otherwise retry only incomplete conditions with backoff for up to five minutes. Both sources must be registered in VST and have searchable embedding documents, and `warehouse-ladder` must have behavior and raw documents required by fusion search. Poll the embedding index with `sensor.id.keyword=<resolved VST UUID>`, the behavior index with `sensor.id.keyword=warehouse-ladder`, and the raw index with `sensorId.keyword=warehouse-ladder`; print each resolved index and final count. Upload completion alone does not prove readiness. Set `VSS_REPO_ROOT=\"${VSS_REPO_ROOT:-$HOME/video-search-and-summarization}\"`, validate its `services/agent/pyproject.toml`, and prove the checked-out host CLI works with `cd \"${VSS_REPO_ROOT}\" && uv run --project \"${VSS_REPO_ROOT}/services/agent\" --no-dev vss search run --help`.",
      "checks": [
        "`curl -sf --max-time 15 http://localhost:8000/health` and `cd \"${VSS_REPO_ROOT}\" && uv run --project \"${VSS_REPO_ROOT}/services/agent\" --no-dev vss search run --help` both returned exit 0 after the checkout marker was validated, the `vss-agent` and `redis` containers were running, and before sample download or ingestion the fully expanded `VST_EXTERNAL_URL` used HTTPS with neither localhost nor an internal IP and reached VST with a bounded GET",
        "`docker ps --format '{{.Names}}' | grep -qx vss-rtvi-vlm` returned exit 0, including in `remote-all` mode where RT-VLM remains local as the media-processing proxy to the remote VLM",
        "A bounded request to the deployment-discovered RT-VLM `/v1/models` endpoint returned exit 0 before sample ingestion",
        "Before upload the agent made setup idempotent by listing through the discovered VST endpoint and, if matching remnants were present, agent-deleting exact or deduplicated remnants of `warehouse_sample`, `warehouse-ladder`, or legacy `sample-warehouse-ladder`; it then downloaded and extracted exactly the pinned `nvidia/vss-developer/dev-profile-sample-data:3.2.0` bundle, ingested only the bytes from `warehouse_sample.mp4` and `sample-warehouse-ladder.mp4` through the agent-backed three-step workflow rather than ingesting every bundled video, used the canonical upload filename `warehouse-ladder.mp4` consistently for the ladder ingestion, validated each separate `/complete` response, and never invoked the deprecated single-step `PUT /api/v1/videos-for-search/{filename}` route",
        "At the end of setup, a bounded `GET <RUNTIME_JSON.vst_url>/vst/api/v1/sensor/list` through the deployment-discovered VST endpoint returned both `warehouse_sample` and the canonically registered `warehouse-ladder` with resolved sensor UUIDs; immediate readiness or bounded retry are both acceptable",
        "At the end of setup, the authoritative `RUNTIME_JSON` resolver reported distinct indexes with `video_embed_index` exactly `mdx-embed-filtered-2025-01-01`, `behavior_index` exactly `mdx-behavior-2025-01-01`, and `raw_index` exactly `mdx-raw-2025-01-01`; the embedding index contained more than zero documents for each resolved video UUID under `sensor.id.keyword`",
        "At the end of setup, `mdx-behavior-2025-01-01` contained more than zero documents for `warehouse-ladder` under `sensor.id.keyword`, and `mdx-raw-2025-01-01` contained more than zero documents for the same canonical source under `sensorId.keyword`; a count from `mdx-embed-filtered-2025-01-01` did not satisfy either behavior or raw readiness"
      ]
    },
    {
      "query": "Find all instances of forklifts in the airport video. The Docker search profile and evaluation fixtures are already prepared on this host.",
      "checks": [
        "The agent successfully listed available video sources from the prepared Docker deployment using deployment-discovered VST/VIOS connectivity before attempting search",
        "Because no registered source matched `airport`, the agent did not invoke `vss search run`, substitute another source, deploy, redeploy, or start an ingestion handshake",
        "The final reply explicitly stated that no `airport` video was registered, listed the available registered sources, and asked the user to clarify the source or explicitly request ingestion"
      ]
    },
    {
      "query": "Find up to three instances of forklifts in the registered sample warehouse source `warehouse_sample`. Use `--query forklifts`, `--search-mode embed`, `--video-source warehouse_sample`, and `--top-k 3`. The Docker search profile and evaluation fixtures are already prepared on this host.",
      "checks": [
        "Before search, the agent listed sources using the prepared Docker deployment's discovered VST/VIOS connectivity and resolved the explicitly requested `warehouse_sample` to that exact registered source",
        "The agent issued a host-side `uv run --project \"${VSS_REPO_ROOT}/services/agent\" --no-dev vss search run` command from the validated checkout that selected `--deployment docker`, `--profile search`, `--search-mode embed`, `--output json`, and `--raw` regardless of option order; it relied on deployment discovery and did not use manual backend endpoints, removed critic/VLM flags, a container/pod shell, or `/generate`",
        "The command completed successfully, passed the forklift query explicitly with `--query` rather than as a positional argument, passed the resolved source with `--video-source`, bounded results with `--top-k 3`, and returned a non-empty `data` array",
        "The final reply was titled `Video Search Results` and reported every hit's source, start time, end time, similarity score, and screenshot or clip URL exactly as returned by the CLI instead of dumping raw JSON",
        "Every cited media URL had the same scheme, host, and effective port as the fully expanded `VST_EXTERNAL_URL` from `generated.env`, used Brev HTTPS rather than localhost or an internal IP on this platform, and succeeded with a bounded HTTP GET of the exact URL without adding a `streamId` routing header",
        "The report distinguished retrieval similarity from visual verification, offered a `Verification Step` without inspecting screenshots absent opt-in, and the agent did not deploy, redeploy, or ingest during this step"
      ]
    },
    {
      "query": "Return up to three candidates for a person wearing a white jacket climbing a ladder in `warehouse-ladder`. Use `--query \"person climbing a ladder\"`, `--search-mode fusion`, `--attribute \"white jacket\"`, `--video-source warehouse-ladder`, and `--top-k 3`. The Docker search profile and evaluation fixtures are already prepared on this host. You are pre-authorized to run the Verification Step autonomously, without asking for confirmation; set `VERIFY_PIXELS=true`, save every returned screenshot under `/tmp/`, and inspect every saved image, then give every inspected hit exactly one verdict label: confirmed, rejected, or uncertain.",
      "checks": [
        "Before search, the agent listed sources using the prepared Docker deployment's discovered VST/VIOS connectivity and resolved `warehouse-ladder` to the exact registered source",
        "The agent issued a host-side `uv run --project \"${VSS_REPO_ROOT}/services/agent\" --no-dev vss search run` command from the validated checkout that selected `--deployment docker`, `--profile search`, `--output json`, and `--raw` regardless of option order; it relied on deployment discovery and did not use manual backend endpoints, removed critic/VLM flags, a container/pod shell, or `/generate`",
        "Through explicit flags or `--decomposed-json`, the command preserved the ladder-climbing action, the `white jacket` attribute, and `search_mode` `fusion`",
        "The command passed the resolved source, bounded results with `top_k` 3, and returned a non-empty `data` array with timestamped hits",
        "The `Video Search Results` report preserved every hit's source, timestamps, score, and media URL, and each downloaded screenshot URL matched the fully expanded `VST_EXTERNAL_URL` origin from `generated.env` and succeeded with a bounded HTTP GET of the exact URL without adding a `streamId` routing header",
        "Without asking again, the agent downloaded every returned screenshot to `/tmp/` and inspected the saved image pixels with its image-inspection capability",
        "The report gave every inspected hit a grounded confirmed, rejected, or uncertain verdict, and the agent did not deploy, redeploy, or ingest during this step"
      ]
    },
    {
      "query": "Return up to three retrieval candidates from the registered `warehouse_sample` source for a neon-pink monster truck. Use `--query \"neon-pink monster truck\"`, `--search-mode embed`, `--video-source warehouse_sample`, and `--top-k 3`. Report the retrieval result faithfully without treating similarity as proof that the object is present. The Docker search profile and evaluation fixtures are already prepared on this host.",
      "checks": [
        "Before search, the agent listed sources using the prepared Docker deployment's discovered VST/VIOS connectivity and resolved the explicitly requested `warehouse_sample` to that exact registered source",
        "The agent issued a host-side `uv run --project \"${VSS_REPO_ROOT}/services/agent\" --no-dev vss search run` command from the validated checkout that selected `--deployment docker`, `--profile search`, `--search-mode embed`, `--output json`, and `--raw` regardless of option order, plus the resolved source, `--top-k 3`, and the neon-pink monster-truck query; it used deployment discovery rather than forbidden endpoints or execution paths",
        "The agent used the actual CLI JSON and reported the exact candidate count plus returned timestamps, scores, and URLs instead of assuming an absent object must yield zero nearest-neighbor candidates",
        "Returned candidates were labeled unverified and were not claimed as proof of presence; zero candidates were not claimed as proof of absence, and visual verification was offered",
        "The agent did not deploy, redeploy, or ingest a source during this step"
      ]
    },
    {
      "query": "Delete the video `warehouse-ladder` from the prepared Docker search deployment. Resolve and save its UUID and canonical name first, reuse the skill's authoritative `RUNTIME_JSON` values, require agent DELETE status `success`, and poll VST absence plus the exact embedding, behavior, and raw index tuples to zero before reporting success.",
      "checks": [
        "The agent listed registered sources and resolved `warehouse-ladder` to its exact VIOS sensor/video UUID before deletion",
        "The agent issued an agent-backed `DELETE /api/v1/videos/<video_id>` through the prepared deployment's discovered agent endpoint using the resolved UUID, required status `success`, and did not use bare VIOS sensor or storage DELETE endpoints as the primary cleanup path",
        "At the end of deletion, a successful source listing through the prepared deployment's discovered VST/VIOS connectivity no longer returned `warehouse-ladder`; immediate absence or bounded retry are both acceptable",
        "The agent reused the authoritative `RUNTIME_JSON` resolver and verified exactly zero embedding documents in `mdx-embed-filtered-2025-01-01` for the resolved UUID using `sensor.id.keyword`, exactly zero behavior documents in `mdx-behavior-2025-01-01` for `warehouse-ladder` using `sensor.id.keyword`, and exactly zero raw documents in `mdx-raw-2025-01-01` for `warehouse-ladder` using `sensorId.keyword`; it did not reuse the embedding index for behavior/raw cleanup checks",
        "The final reply reported the DELETE status and the actual VIOS, embedding, behavior, and raw cleanup results"
      ]
    },
    {
      "scenario": "kubernetes-ingress-contract",
      "query": "Explain the exact host-side commands you would use to list registered sources and search the Kubernetes search profile exposed at `VSS_PUBLIC_URL=https://vss-search.example.com`. Search only the resolved `warehouse_sample` source for forklifts with embed mode and top-k 3. This is a contract check: show the commands but do not execute them or deploy anything.",
      "checks": [
        "The answer trims the VSS_PUBLIC_URL trailing slash, uses `https://vss-search.example.com/vst/api/v1/sensor/list` for source listing, and uses `POST https://vss-search.example.com/generate` for search",
        "The `/generate` JSON body contains `input_message` and preserves the resolved `warehouse_sample` source, forklift query, embed mode, and top-k 3",
        "The answer does not use kubectl, port-forward, Kubernetes Service DNS, a NodePort, localhost, direct `:8000`, direct `:9200`, direct `:30888`, `discover_kubernetes`, or `vss search run --deployment kubernetes`",
        "The answer explains that Elasticsearch and RTVI are private Agent dependencies in Kubernetes and does not propose exposing them for the operate skill"
      ]
    }
  ]
}
